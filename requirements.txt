# ─────────────────────────────────────────────────────────────
# StudyLens — Notes Generator  |  requirements.txt
#
# Install:  pip install -r requirements.txt
#
# System packages also required:
#   sudo apt-get install -y tesseract-ocr poppler-utils
# ─────────────────────────────────────────────────────────────

# Web framework
flask==3.0.3
python-dotenv==1.0.1
werkzeug==3.0.3

# OpenAI SDK (for AI notes — optional, fallback works without it)
openai==1.51.0

# httpx pinned: openai 1.x passes `proxies` to httpx;
# httpx >= 0.28 removed that kwarg → startup crash.
httpx==0.27.2

# PDF text extraction (primary — fast, no Tesseract needed for digital PDFs)
pdfplumber==0.11.4

# OCR fallback (for scanned PDFs)
pytesseract==0.3.13
Pillow==10.4.0
pdf2image==1.17.0       # converts PDF pages to images for Tesseract

# Transitive dependency pins for reproducibility
anyio==4.4.0
certifi>=2024.2.2
charset-normalizer>=3.3.2
distro==1.9.0
h11==0.14.0
httpcore==1.0.5
idna>=3.7
jiter==0.5.0
pydantic==2.8.2
pydantic-core==2.20.1
sniffio==1.3.1
tqdm==4.66.5
typing_extensions>=4.12.2

#scraper requirements
requests
beautifulsoup4
tqdm
